/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package custom.spring.library;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.ApplicationContext;


@SpringBootTest
class LibraryTests {

    @Autowired
    CustomLibraryService customLibraryService;

    @Autowired
    ApplicationContext applicationContext;

    @Test
    void someTest() {
        CustomLibraryService customLibraryService1 = (CustomLibraryService) applicationContext.getBean("customLibraryService");
        customLibraryService1.printSome("!!!");
        doSome();
        customLibraryService.printSome("dfssf");
    }

    @LogExecutionTime
    private void doSome() {

    }
}
